# Netlify-specific environment variables
NETLIFY=true
NETLIFY_NEXT_PLUGIN_SKIP=true

# Next.js settings
NEXT_PUBLIC_BASE_URL=https://your-netlify-site-url.netlify.app
NODE_ENV=production

# Copy all other settings from your .env file but update any URLs to use your Netlify domain
# You will need to add these in your Netlify dashboard as environment variables as well 